@attribute [Authorize]

@page "/tiposproceso/crear-editar/{Id:int?}"
@inherits CrearEditarTipoProcesoBase
@layout AdminLayout

<div class="container mt-4 tipo-proceso-form">
    <h3>@(Id.HasValue ? "✏️ Editar Tipo de Proceso" : "➕ Nuevo Tipo de Proceso")</h3>

    <EditForm Model="@Tipo" OnValidSubmit="Guardar">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <div class="mb-3">
            <label class="form-label">Nombre</label>
            <InputText class="form-control" @bind-Value="Tipo.Nombre" />
        </div>

        <button class="btn btn-primary me-2" type="submit">💾 Guardar</button>
        @if (Id.HasValue)
        {
            <button class="btn btn-danger" type="button" @onclick="Eliminar">🗑️ Eliminar</button>
        }
        <button class="btn btn-secondary ms-2" type="button" @onclick="Cancelar">Cancelar</button>
    </EditForm>
</div>
